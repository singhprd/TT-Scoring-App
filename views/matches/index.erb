<div id="content">

<% @matches.each do |match| %>

<p>match:</p>

<%= Player.find(match.player_winner).name %>
v
<%= Player.find(match.player_looser).name %>

<p><a href="/matches/<%= match.id %>">
    <%= match.winner_points %>
    v
    <%= match.looser_points %>
</a>
  </p>
  <hr>
  
<% end %>

</div>

<a class="bottom-link" href="/matches/new">Add New matches</a>